@model UsluzniObrt.MVC.ViewModels.ItemCreateViewModel
@{
    ViewBag.Title = "CreateItem";
}

@using (Html.BeginForm("CreateITem", "Admin", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary("", new { @class = "text-danger" })

    <div class="form-group">
        @Html.LabelFor(m => m.Naziv, new { @class = "col-md-3 control-label" })
        <div class="col-md-9">
            @Html.TextBoxFor(m => m.Naziv, new { @class = "form-control" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Cijena, new { @class = "col-md-3 control-label" })
        <div class="col-md-9">
            @Html.TextBoxFor(m => m.Cijena, new { @class = "form-control" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.CategoryId, new { @class = "control-label" })
        <div class="col-md-9">
            @Html.DropDownListFor(x => x.CategoryId, new SelectList(ViewBag.CategoryList, "CategoryId", "Name"))
            @Html.ValidationMessageFor(m => m.CategoryId)
        </div>
    </div>

    <div class="form-group">
        <!-- Button -->
        <div class="col-md-offset-3 col-md-9">
            <input type="submit" class="btn btn-success" value="Create Item" />
        </div>
    </div>
}